import React from 'react'
import { render } from '@testing-library/react'

import {{pascalCase name}} from './{{pascalCase name}}'

describe('{{pascalCase name}}', () => {
  test('renders the {{pascalCase name}} component', () => {
    render(<{{pascalCase name}} />)
  })
})
